@model Voucher
@{
    ViewData["Title"] = "AddNewVoucher";
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Voucher</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: beige; /* Set the background color for the entire page */
        }

        .container {
            text-align: center;
            position: absolute;
            top: 65%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

        .form-container {
            background-color: white;
            box-shadow: 0px 0px 10px 0px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            padding: 20px;
            width: 400px;
            display: inline-block;
            text-align: left;
        }

        form {
            margin-bottom: 30px;
        }

        h1 {
            color: #007bff;
        }

        label {
            display: block;
            margin-top: 10px;
            margin-bottom: 5px;
        }

        input {
            width: 100%;
            padding: 8px;
            box-sizing: border-box;
            margin-bottom: 10px;
        }

            input[type="file"] {
                margin-top: 5px;
                margin-bottom: 15px;
            }

        button {
            background-color: #28a745;
            color: #fff;
            border: none;
            padding: 10px 20px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin-top: 20px;
            cursor: pointer;
            border-radius: 5px;
        }

            button:hover {
                background-color: #0056b3;
            }
    </style>
</head>

<body>
    <div class="container">
        <div class="form-container">
            <h1>Add New Voucher</h1>
            <hr />
            <form method="post" asp-action="AddVoucher" asp-controller="Admin" enctype="multipart/form-data">
                <label asp-for="VoucherName">Voucher Name</label>
                <input asp-for="VoucherName" />

                <label asp-for="VoucherValue">Voucher Value</label>
                <input asp-for="VoucherValue" />

                <label asp-for="VoucherPrice">Voucher Price</label>
                <input asp-for="VoucherPrice" />

                <label asp-for="VoucherLink">Voucher Image</label>
                <input type="file" name="imagefile" accept="image/*" />

                <button>Add New Voucher</button>
            </form>

            
        </div>
        <form method="post" asp-action="Index" asp-controller="Admin">
            <div>
                <button type="submit">Back to Home</button>
            </div>
        </form>
    </div>
</body>

</html>
